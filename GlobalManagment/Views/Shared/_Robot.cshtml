<div id="robot-container">
    <img id="robot-image" src="" />


    <div id="robot-text">
        <div class="right-curtain"></div>
    </div>

</div>

<style>  
#robot-text {
    margin-top: 10px;
    font-size: 20px;
    font-weight: bold;
    color: #fff;
    text-align: center;
    padding: 28px;
    border-radius: 75px;
    background: linear-gradient(to bottom, #4b0082 30%, #000 100%); /* Stage backdrop */
    box-shadow: 0px 20px 30px rgba(0, 0, 0, 0.8); /* Depth for the stage */
    position: relative;
    width:auto;
    height:auto;
    opacity:0.8;
}

/* Stage floor */
#robot-text::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 20px;
    background: linear-gradient(to bottom, #000, #333); /* Stage floor effect */
    box-shadow: 0px -5px 10px rgba(0, 0, 0, 0.6); /* Shadow between the floor and the backdrop */
}

/* Curtains on the sides */
/* #robot-text::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(255, 0, 0, 0.9) 20%, rgba(139, 0, 0, 1) 70%);
    clip-path: polygon(0 0, 20% 0, 10% 100%, 0% 100%); /* Curtain on the left */
} */

#robot-text .right-curtain {
    position: absolute;
    top: 0;
    right: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(circle, rgba(255, 0, 0, 0.9) 20%, rgba(139, 0, 0, 1) 70%);
    clip-path: polygon(80% 0, 100% 0, 100% 100%, 90% 100%); /* Curtain on the right */
}

/* Spotlight effect */
#robot-text .spotlight {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 150px;
    height: 200%;
    background: radial-gradient(circle, rgba(255, 255, 0, 0.4) 0%, rgba(255, 255, 0, 0.1) 70%, transparent 100%);
    animation: spotlightMove 3s infinite alternate ease-in-out; /* Spotlight animation */
}
</style>